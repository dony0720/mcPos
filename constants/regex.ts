/**
 * 정규식 패턴 상수들
 * 프로젝트 전체에서 사용되는 정규식 패턴을 중앙 관리
 */

/**
 * 현재 프로젝트에서 사용 중인 정규식 패턴들
 */
export const REGEX_PATTERNS = {
  // 숫자만 추출 (콤마, 공백, 특수문자 제거) - 가격 입력에서 사용
  NUMBERS_ONLY: /[^0-9]/g,
} as const;
